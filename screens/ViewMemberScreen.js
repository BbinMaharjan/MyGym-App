import React from 'react';
import {View, Text, StyleSheet} from 'react-native';

const ViewMemberScreen = props => {
  return (
    <View>
      <Text>ViewMemberScreen</Text>
    </View>
  );
};

const styles = StyleSheet.create({});
export default ViewMemberScreen;
